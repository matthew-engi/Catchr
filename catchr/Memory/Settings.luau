--!strict

local Settings = {
	-- Memory Settings -------
	memoryRange = 200, -- (200 Max) How far it looks for UIDs
	memoryLifetime = 7 * 86400, -- Seven days lifespan
	identifierName = "catchr"
}

return Settings